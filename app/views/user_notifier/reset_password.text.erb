We received a request to reset your Drinkalculator password.  To do so, please follow the link below:

<%= reset_url(@user.code) %>

If you received this email in error, then no action is required.  The above link will expire at <%= @user.expires_at.strftime("%-I:%M on %a, %-d %b %Y") %>

Thank you

Drinkalculator